import { ProgressBarItem } from "./Item";
import { ProgressBarRoot } from "./Root";

export const ProgressBar = {
  Item: ProgressBarItem,
  Root: ProgressBarRoot,
};

export type ProgressBarProps = ProgressBarRoot;
